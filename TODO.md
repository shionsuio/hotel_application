# ホテル予約API TODO

更新日: 2026-09-13

面接でJavaのバックエンド設計を説明するためのプロジェクト。
セキュリティ・性能・冪等性・同時予約の整合性を重視し、一つずつ理解しながら実装する。
完了した項目は `[ ]` を `[x]` に変更する。コード内の `// TODO` は具体的な実装メモとして使う。

## 決定済みのルール

- [x] 宿泊期間を `LocalDate` で扱う
- [x] チェックイン・アウト時刻は統一し、予約リクエストでは時刻を受け取らない
- [x] チェックイン日を含み、チェックアウト日を含まない期間として扱う
- [x] 既存予約のチェックアウト当日に、次の予約のチェックインを許可する

## DBの基礎

- [x] H2で `schema.sql` によるテーブル作成を行う
- [x] `rooms` テーブルを作成する（ID・部屋番号・1泊料金）
- [x] `reservations` テーブルを作成する（ID・部屋ID・宿泊期間・ステータス）
- [x] 主キー・必須項目・部屋番号の一意制約・外部キーを設定する
- [x] 料金が0以上、チェックイン日 < チェックアウト日となる制約を設定する
- [x] ステータスを `CONFIRMED` / `CANCELED` に制限する
- [x] `data.sql` に3部屋のサンプルデータを用意する
- [ ] DBの変更履歴をマイグレーションで管理する（現在の `schema.sql` とは別）
- [ ] 本番用DBを決定し、接続設定と開発・テスト環境を整える

## 空室検索

- [x] 検索リクエスト・レスポンスを定義する
- [x] `SearchRoomController` → `SearchRoomService` → `RoomRepository` を接続する
- [x] `JdbcRoomRepository` を実装し、検索結果を `List<Room>` に変換する
- [x] `NOT EXISTS` で期間が重なる有効な予約がない部屋を取得する
- [x] `CONFIRMED` のみを重複判定の対象にする
- [x] SQLの値をプレースホルダーで渡す
- [ ] 必須日付・期間の前後関係・最大検索期間を検証する
- [ ] 検索結果の上限・ページングを実装する
- [ ] HTTP経由で検索APIのリクエスト・レスポンスを確認する

### 検索テスト

以下の完了済み4件は、2026-09-13に `JdbcRoomRepositoryTest` の実行結果で成功を確認。
RepositoryとH2を使う統合テストであり、HTTP経由のテストはまだ行っていない。

- [x] 予約が0件なら3部屋すべて返る
- [x] 期間が重なる予約がある部屋は返らない
- [x] 既存予約のチェックアウト当日は空室として返る
- [x] キャンセル済み予約は、期間が重なっていても空室として返る
- [ ] 希望チェックアウト日が既存予約のチェックイン日と同じでも空室として返る（次に取り組む）
- [ ] 同一期間・包含関係のある期間でも重複を判定できる
- [ ] 全部屋が予約済みなら空のリストが返る
- [ ] 不正な検索リクエストに対するHTTPレスポンスを確認する

## 予約作成

- [x] 予約リクエストと `POST /reservations` の入口を作成する
- [ ] `ReservationService` と予約用Repositoryを作成する（Controllerは現在 `null` を返す）
- [ ] 必須項目・部屋IDの正数・期間の前後関係・過去日・最大宿泊期間を検証する
- [ ] 部屋の存在と予約時点の空室を確認する
- [ ] 予約をDBへ保存し、予約IDを含むレスポンスを返す
- [ ] 空室確認と予約保存を同一トランザクションで行う
- [ ] 入力不正・部屋なし・予約競合を400・404・409へ変換する共通例外処理を用意する

## ロック・二重予約防止

空室検索の結果だけでは予約確定を保証しない。現在のDB制約だけでは期間が重なる予約を防げない。

- [ ] ロック対象・取得順序・タイムアウトを決める
- [ ] 同一トランザクション内で部屋をロックし、期間重複を再確認して予約を保存する
- [ ] 同じ部屋・重なる期間への並行予約で、成功が1件だけになることを確認する
- [ ] ロック待ち・タイムアウト時の処理とレスポンスを確認する
- [ ] 採用する本番DBと同じ種類のDBでロックを検証する（H2だけで判断しない）

## 冪等性

同じ操作の再送への対応。異なる利用者による二重予約防止とは別に実装する。

- [ ] `Idempotency-Key` を受け取る
- [ ] 利用者とキーの組にDBの一意制約を設ける
- [ ] 冪等性の記録と予約を同一トランザクションで保存する
- [ ] 同じキー・同じ内容の再送には同じ結果を返す
- [ ] 同じキー・異なる内容のリクエストは拒否する
- [ ] 逐次再送・同時送信で予約が1件になることを確認する
- [ ] 異なる利用者間では同じキーを独立して扱えることを確認する
- [ ] 保存途中の失敗で予約と冪等性の記録が両方ロールバックされることを確認する

## 認証・認可

Spring Securityの依存関係は導入済み。アプリ固有の認証・認可はこれから実装する。

- [x] 予約リクエストの `userId` を除外する
- [ ] 認証方式とSecurity設定を決める
- [ ] 認証情報から予約者を取得し、予約データに関連付ける
- [ ] 未認証アクセスの拒否を確認する
- [ ] 予約参照・変更・キャンセルAPIの追加時に、所有者・権限を確認する
- [ ] 他人の予約への不正アクセスを拒否するテストを追加する

## 性能・面接向けの説明

- [ ] データ量・同時アクセス数・測定条件を決める
- [ ] 空室検索の実行計画と応答時間を測定する
- [ ] 測定結果から検索用インデックスを検討し、改善前後を比較する
- [ ] 予約処理のロック待ちが性能に与える影響を測定する
- [ ] READMEに起動方法・APIの使い方・テスト実行方法を書く
- [ ] DB制約・トランザクション・ロック・冪等性の役割と採用理由を説明する
- [ ] 検証結果と、未対応の制約・課題を記録する

## TODOとコメントの整理

- [ ] 完了済みのコード内TODOを削除するか、設計理由のコメントへ変更する
- [ ] テスト名を英語メソッド名と日本語の `@DisplayName` に統一する
- [ ] 古い `checkInTime()` の説明を `checkInDate()` に更新する
